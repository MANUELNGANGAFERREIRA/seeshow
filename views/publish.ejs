<!DOCTYPE html>
<html>
<head>
  <title>Publicar</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h2>Usuário: <%= user %></h2>
    <h3>Pontos disponíveis: <%= points %></h3>

    <form action="/publish" method="POST">
      <input type="hidden" name="user" value="<%= user %>">

      <input name="title" placeholder="Título da publicação" required><br>
      <textarea name="content" placeholder="Conteúdo da publicação" required></textarea><br>
      <input name="redirectUrl" placeholder="Link de redirecionamento" required><br>
      <button type="submit">🚀 Publicar</button>
    </form>

    <a class="btn" href="/?user=<%= user %>">👈 Ver Publicações</a>
  </div>
</body>
</html>
